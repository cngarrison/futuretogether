/**
 * site/utils/constants.ts
 *
 * Platform-wide constants. For environment-specific values
 * (e.g. the Global group UUID), use site/utils/settings.ts.
 */

/** Slug for the FT Global group — deterministic across all environments. */
export const FT_GLOBAL_GROUP_SLUG = "ft-global";

/** Slug of the main recurring meetup program within the FT Global group. */
export const FT_GLOBAL_GROUP_PROGRAM_SLUG = "discuss-our-future";
